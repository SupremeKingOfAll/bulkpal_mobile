import "package:flutter/material.dart";

class AppColours {
  static const primaryColour = Color(0xFF02040A); // Deep dark background
  static const cardColour = Color(
    0xFF0D121F,
  ); // Slightly lighter card background
  static const accentColour = Color(
    0xFF2DD4BF,
  ); // Teal accent for success/progress
  static const secondaryAccent = Color(
    0xFF3B82F6,
  ); // Blue for calorie progress bar
  static const textColour = Color(0xFFFFFFFF); // Pure white for main headings
  static const textSecondary = Color(0xFF94A3B8); // Muted slate for labels
  static const iconBackground = Color(
    0xFF1E293B,
  ); // Dark slate for icon containers
  static const navActive = Color(
    0xFF2DD4BF,
  ); // Teal for active navigation state

  static const borderColour = Color(0xFF1A2433); //Border / divider colour
  static const surfaceHighlight = Color(0xFF0B3A3F);
  static const errorColour = Color(0xFFEF4444);
  static const successColour = Color(0xFF388E3C);
}
